import { AuthService } from '@auth0/auth0-angular';

@Component({
  selector: 'app-sli',
  templateUrl: './sli.component.html',
  styleUrls: ['./sli.component.scss'],
})
export class SliComponent implements OnInit {
  constructor(
    public auth: AuthService,
  ) {}

  ngOnInit(): void {
    this.checkIfUserLoggedIn()
  }
  
  checkIfUserLoggedIn(): void {
    this.auth.isAuthenticated$.subscribe((success: boolean) => {
      
    }
  }
  
}
